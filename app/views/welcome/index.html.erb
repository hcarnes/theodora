<h2>Welcome to <%= current_organization && current_organization.name || "Theodora" %></h2>
<p>
  The time is now: <%= Time.now %>
</p>
<% if user_signed_in? %>
  <p>
    <%= link_to('Change Organization', tenant_selector_path) %>
  </p>
  <p>
    <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
  </p>
<% else %>
  <p>
    <%= link_to('Login', new_user_session_path)  %>  
  </p>
<% end %>